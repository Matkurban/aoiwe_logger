name: example
description: A sample command-line application.
version: 1.0.0

environment:
  sdk: ^3.11.3

# Add regular dependencies here.
dependencies:
  aoiwe_logger:
    path: ../

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.0
